# OpenClaw Manager 智能体规则

## 交流方式

- 默认使用中文回答。
- 不要一次性给出大量步骤，除非用户明确要求完整操作手册。
- 每次只推进当前最必要的一步，等待用户确认后再继续。
- 涉及服务器、Docker、Git、数据库迁移或生产环境操作时，先说明风险，再给出或执行命令。

## 修改规则

- 修改代码前先检查现状，说明准备修改的文件并等待用户确认。
- 不重构无关代码，不删除已有配置、注释和脚本，除非明确说明原因并获得确认。
- 优先复用现有实现、标准库和平台原生能力；避免为未来需求提前增加抽象。
- 修复缺陷时先查找所有调用方，在共享根因处做最小修复。
- 不降低输入校验、安全边界、错误处理、数据完整性和可访问性要求。
- 非简单逻辑应留下最小可运行测试。
- 每条新增或修改的可执行规则都必须有对应的自动化测试；影响生产一致性的规则还必须同步更新校验脚本。
- 代码、配置、部署流程或用户可见行为发生变化时，必须同步更新相关文档；确实无需更新时，应在 PR 描述中说明原因。
- 修改完成后优先提供 `git diff` 或简明变更摘要。

## Git 与 PR

- 未经用户明确授权，不得 push、merge、部署、删除分支或改写 Git 历史。
- Commit message 使用：`type: English description/中文说明`。
- 示例：`feat: add control plane metadata model/增加控制平面元数据模型`。
- PR 标题沿用相同的中英文格式。
- PR 描述必须严格按照 `.github/pull_request_template.md` 填写全部章节，不得自行替换模板结构。
- 提交前检查实际 diff、工作区状态和测试结果，避免夹带无关改动。

## 安全与运维

- 对生产环境操作保持保守，优先执行只读检查。
- 执行 `rm`、`chmod`、`chown`、Docker volume 操作、数据库迁移等可能破坏数据的命令前，必须说明影响和恢复方式。
- 不得接受或拼接来自前端的任意容器名、用户目录或 Shell 命令。
- 实例操作必须基于服务端解析的实例标识并完成权限校验。
- 不得把真实密钥、令牌、密码或校内私有配置提交到仓库。
